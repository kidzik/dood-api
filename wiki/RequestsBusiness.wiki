#summary Business requests.

= Business requests =

== `/business/get/` ==
Starts session.

*Method:*
GET

*Prameters:*
|| *parameter* || *type* || *description* ||
|| `id` (optional) || `Integer` || Business id ||
|| `what` (optional) || `String` || Search phraze ||
|| `where` (optional) || `String` || Search location ||

*Returns:*
[DatatypesBusiness Business] structures.